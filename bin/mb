#!/usr/bin/env ruby

require_relative '../lib/master_blaster'
require 'thread'

queue = Queue.new

feeder = Thread.new { JobFeeder.new(queue).run }

mb = MasterBlaster.new(ENV['DATABASE_URL'], queue)
mb.run
